#Day 21 — SPEED TYPING (LEVEL 2)
#You must rewrite all FOUR from scratch:

#Ansible:

#Install nginx
#Start + enable service

---
- name: Speed typing Nginx deployment
  hosts: all
  become: yes
  tasks:
    - name: Install nginx
      yum:
        name: nginx
        state: present

    - name: Start and enable nginx service
      service:
        name: nginx
        state: started
        enabled: yes



#Terraform:
#Create EC2 (choose any Pathnex-approved type)
#Tag it with: Owner: PathnexStudent
#Kubernetes:

#Deployment with 3 replicas
#Image: nginx


resource "aws_instance" "PathnexSpeedEC2" {
  ami           = "ami-0abcd1234abcd1234"
  instance_type = "t3.medium"

  tags = {
    Owner = "PathnexStudent"
  }
}



apiVersion: apps/v1
kind: Deployment
metadata:
  name: speed-nginx
spec:
  replicas: 3
  selector:
    matchLabels:
      app: nginx
  template:
    metadata:
      labels:
        app: nginx
    spec:
      containers:
        - name: nginx
          image: nginx

          

#Shell:

#Print date, uptime, disk usage
#⏱ TIME LIMIT: 15 minutes



#!/bin/bash
echo "=== Current Date & Time ==="
date

echo -e "\n=== System Uptime ==="
uptime

echo -e "\n=== Disk Usage ==="
df -h /



#Multi-Environment Pipeline
#🔹 Jenkins Pipeline — Deploy to Multiple Environments
#You will learn how to define multiple environment deployments in one pipeline.

#pipeline { agent any environment { INSTITUTE_NAME = "Pathnex" } 
#stages { stage('Deploy to Dev') { steps { sh 'echo "Deploying
# $INSTITUTE_NAME to Dev environment"' } } stage('Deploy to Staging') 
# { steps { input message: "Approve Staging Deployment?" sh 'echo 
# "Deploying $INSTITUTE_NAME to Staging environment"' } } stage('Deploy to Prod') 
# { steps { input message: "Approve Prod Deployment?" sh
#  'echo "Deploying $INSTITUTE_NAME to Prod environment"' } } } }

pipeline {
    agent any
    environment {
        INSTITUTE_NAME = "Pathnex"
    }
    stages {
        stage('Deploy to Dev') {
            steps {
                sh 'echo "Deploying $INSTITUTE_NAME to Dev environment"'
            }
        }
        stage('Deploy to Staging') {
            steps {
                input message: "Approve Staging Deployment?"
                sh 'echo "Deploying $INSTITUTE_NAME to Staging environment"'
            }
        }
        stage('Deploy to Prod') {
            steps {
                input message: "Approve Prod Deployment?"
                sh 'echo "Deploying $INSTITUTE_NAME to Prod environment"'
            }
        }
    }
}



#🔹 GitLab CI — Multi-Environment
#You will learn how to define multiple environment deployments in
# GitLab CI.

#stages:

#deploy
#variables: INSTITUTE_NAME: "Pathnex"

#deploy-dev: stage: deploy image: alpine:latest script: - echo
#"Deploying $INSTITUTE_NAME to Dev environment"
#deploy-staging: stage: deploy image: alpine:latest script:
# - echo "Deploying $INSTITUTE_NAME to Staging environment" when:
#  manual
#deploy-prod: stage: deploy image: alpine:latest script: - 
#echo "Deploying $INSTITUTE_NAME to Prod environment" when:
# manual



stages:
  - deploy

variables:
  INSTITUTE_NAME: "Pathnex"

deploy-dev:
  stage: deploy
  image: alpine:latest
  script:
    - echo "Deploying $INSTITUTE_NAME to Dev environment"

deploy-staging:
  stage: deploy
  image: alpine:latest
  script:
    - echo "Deploying $INSTITUTE_NAME to Staging environment"
  when: manual

deploy-prod:
  stage: deploy
  image: alpine:latest
  script:
    - echo "Deploying $INSTITUTE_NAME to Prod environment"
  when: manual